#!/bin/bash
set -euo pipefail

TOPIC=$(sed -n 's/^topic *= *"\(.*\)"/\1/p' "$(dirname "$0")/config.toml")

curl -s -o /dev/null \
  -H "Title: $1" \
  -H "Priority: high" \
  -H "Tags: movie_camera" \
  -d "$2" \
  "https://ntfy.sh/$TOPIC"
